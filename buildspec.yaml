
phases:
  pre_build:
    commands:
      - echo Logging into ECR
      - aws ecr get-login-password --region ${AWS_DEFAULT_REGION} | docker login --username project3user --password-stdin ${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_DEFAULT_REGION}.amazonaws.com
